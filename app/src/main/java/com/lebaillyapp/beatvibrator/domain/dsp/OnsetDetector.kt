package com.lebaillyapp.beatvibrator.domain.dsp

/**
 * Détecteur d'onsets (transitoires)
 */
class OnsetDetector {
    /**
     * Détecte les positions d'onsets dans un signal
     */
    fun detectOnsets(samples: FloatArray): List<Int> {
        // TODO: implémenter la détection d'onsets
        return emptyList()
    }
}