package com.lebaillyapp.beatvibrator.domain.dsp

/**
 * Calculateur RMS (Root Mean Square) sur fenêtres audio
 */
class RmsCalculator {
    /**
     * Calcule la liste des valeurs RMS sur des fenêtres glissantes
     */
    fun computeRmsOverWindows(samples: FloatArray, windowSize: Int, hopSize: Int): List<Float> {
        // TODO: implémenter le calcul RMS
        return emptyList()
    }
}